# Fix README Duplicate Tagline

## Summary

Fixed duplicate tagline in README.md after rebase on main branch.

## Problem

After rebasing feature/npx-cli on main, README.md had duplicate taglines:

```markdown
# TalkReplay

**The only tool that replays Claude, Codex, Cursor, AND Gemini sessions in one unified UI**

> **The only tool that replays Claude, Codex, Cursor, AND Gemini sessions in one unified UI.**
```

## Solution

Removed the old bold tagline, keeping only the blockquote version.

## Files Changed

- `README.md` - Removed duplicate tagline line

## Code Highlights

```markdown
# TalkReplay

> **The only tool that replays Claude, Codex, Cursor, AND Gemini sessions in one unified UI.**
```

## Self-Tests

```bash
npx prettier --check README.md  # OK
```
